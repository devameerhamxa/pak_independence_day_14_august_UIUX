import 'package:flutter/material.dart';

class AppColors {
  static const Color pakGreen = Color(0xFF00401A); // Deeper, richer green
  static const Color pakWhite = Color(
    0xFFE0E0E0,
  ); // Slightly off-white for a softer look
  static const Color background = Color(
    0xFF0A0A0A,
  ); // Deep black for a dramatic feel
  static const Color accent = Color(0xFF1A1A1A); // Dark accent for depth
  static const Color gold = Color(0xFFD4AF37); // Striking gold for highlights
  static const Color charcoal = Color(0xFF333333); // For UI elements
}
